{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/ara-registry/spec/refs/heads/main/ara.schema.json",
  "title": "ara.json defining an ARA package",
  "description": "Schema for an ARA package manifest. Used for discovery, installation, and configuration of AI development artifacts.",
  "type": "object",
  "required": ["name", "version", "description", "author", "tags"],
  "properties": {
    "$schema": {
      "type": "string",
      "format": "uri",
      "description": "JSON Schema URI for this ara.json format",
      "example": "https://raw.githubusercontent.com/ara-registry/spec/refs/heads/main/ara.schema.json"
    },
    "specVersion": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+$",
      "description": "Version of the ARA spec this manifest targets.",
      "default": "1.0"
    },
    "name": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9_-]+/[a-zA-Z0-9_-]+$",
      "minLength": 3,
      "maxLength": 200,
      "description": "Package name in namespace/package-name format. Must contain exactly one forward slash separating namespace from package name.",
      "example": "acme/weather-agent"
    },
    "version": {
      "type": "string",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-((?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\\.(?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\\+([0-9a-zA-Z-]+(?:\\.[0-9a-zA-Z-]+)*))?$",
      "description": "Semantic version string (e.g., '1.0.0', '2.1.0-alpha'). Must follow semantic versioning format.",
      "example": "1.0.0"
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "maxLength": 500,
      "description": "Human-readable explanation of package functionality. Should focus on capabilities, not implementation details.",
      "example": "AI agent for automated code review with support for multiple languages"
    },
    "author": {
      "oneOf": [
        {
          "type": "string",
          "format": "email",
          "description": "Author email address (string form, backwards compatible).",
          "example": "developer@example.com"
        },
        {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "Author or organization name."
            },
            "email": {
              "type": "string",
              "format": "email",
              "description": "Author email address."
            },
            "url": {
              "type": "string",
              "format": "uri",
              "description": "Author or organization homepage URL."
            }
          },
          "additionalProperties": false
        }
      ],
      "description": "Author contact information. Accepts a plain email string or an object with name, email, and url."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 50,
        "pattern": "^[a-zA-Z0-9_-]+$"
      },
      "minItems": 1,
      "description": "Discovery tags for search and categorization. Use lowercase, hyphen-separated terms.",
      "example": ["code-review", "automation", "quality"]
    },
    "type": {
      "type": "string",
      "enum": [
        "agent",
        "mcp-server",
        "powers",
        "steering",
        "skill",
        "context",
        "agents-md"
      ],
      "description": "Package type determining installation and usage behavior."
    },
    "platform": {
      "type": "string",
      "description": "Optional vendor-specific platform identifier (e.g., 'kiro', 'claude-code'). Allows tool vendors to declare platform-specific behavior while remaining interoperable at the registry level.",
      "example": "kiro"
    },
    "files": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "File or directory path relative to package root"
      },
      "description": "Package files to include. If omitted, all files in the package directory are included."
    },
    "license": {
      "type": "string",
      "maxLength": 100,
      "description": "SPDX license identifier (e.g., 'MIT', 'Apache-2.0').",
      "example": "MIT"
    },
    "homepage": {
      "type": "string",
      "format": "uri",
      "description": "Project homepage or documentation URL.",
      "example": "https://example.com/my-package"
    },
    "repository": {
      "type": "string",
      "format": "uri",
      "description": "Source repository URL for browsing and contributing.",
      "example": "https://github.com/example/my-package"
    },
    "private": {
      "type": "boolean",
      "default": false,
      "description": "When true, registries MUST refuse to publish this package to any public index."
    },
    "dependencies": {
      "type": "object",
      "additionalProperties": {
        "type": "string",
        "description": "Version constraint following semantic versioning (e.g., '^1.0.0', '>=2.0.0')"
      },
      "description": "Package dependencies with version constraints. Keys are package names in namespace/package-name format."
    },
    "sources": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/PackageSource"
      },
      "description": "Installation sources for MCP server packages. Only valid when type is 'mcp-server'. Clients attempt sources in order, preferring those marked with 'preferred: true'."
    }
  },
  "definitions": {
    "PackageSource": {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["npm", "pypi", "git", "mcp-registry", "oci"],
          "description": "Source type indicating how to install the package"
        },
        "package": {
          "type": "string",
          "description": "Package identifier in the source registry (npm, pypi, mcp-registry)"
        },
        "version": {
          "type": "string",
          "description": "Package version. Must be a specific version, not a range."
        },
        "registry": {
          "type": "string",
          "description": "Registry URL or host. Defaults to the public registry for the source type."
        },
        "repository": {
          "type": "string",
          "format": "uri",
          "description": "Git repository URL. Required for git source type."
        },
        "ref": {
          "type": "string",
          "description": "Git ref (tag, branch, or commit hash). Used with git source type."
        },
        "subfolder": {
          "type": "string",
          "description": "Subfolder within the repository for monorepo structures. Used with git source type."
        },
        "installCommand": {
          "type": "string",
          "description": "Build command to run after cloning. Used with git source type. Clients MUST require explicit user confirmation before executing.",
          "example": "npm install && npm run build"
        },
        "executable": {
          "type": "string",
          "description": "Path to executable after build. Used with git source type.",
          "example": "dist/index.js"
        },
        "preferred": {
          "type": "boolean",
          "description": "Mark this source as preferred. Clients attempt preferred sources first."
        },
        "image": {
          "type": "string",
          "description": "Full OCI image name (e.g., ghcr.io/acme/weather-mcp). Required for oci source type."
        },
        "tag": {
          "type": "string",
          "description": "OCI image tag. Defaults to 'latest'. Used with oci source type."
        },
        "digest": {
          "type": "string",
          "description": "Content-addressable OCI digest (preferred over tag for reproducibility). Used with oci source type.",
          "example": "sha256:abc123..."
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "type": { "const": "npm" } }
          },
          "then": {
            "required": ["package"]
          }
        },
        {
          "if": {
            "properties": { "type": { "const": "pypi" } }
          },
          "then": {
            "required": ["package"]
          }
        },
        {
          "if": {
            "properties": { "type": { "const": "git" } }
          },
          "then": {
            "required": ["repository"]
          }
        },
        {
          "if": {
            "properties": { "type": { "const": "mcp-registry" } }
          },
          "then": {
            "required": ["package"]
          }
        },
        {
          "if": {
            "properties": { "type": { "const": "oci" } }
          },
          "then": {
            "required": ["image"]
          }
        }
      ]
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "type": { "const": "mcp-server" } }
      },
      "then": {
        "properties": {
          "sources": {
            "description": "Installation sources for this MCP server"
          }
        }
      },
      "else": {
        "properties": {
          "sources": {
            "not": {},
            "description": "sources field is only allowed for mcp-server type"
          }
        }
      }
    }
  ]
}
